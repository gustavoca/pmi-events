'use strict';

module.exports = function(participant) {
  // participant.observe('before save', function updateTimestamp(ctx, next) {
  //   if (ctx.isNewInstance) { //New participant, generate qrcode
  //     // ctx.instance.names =  'UPDATE New coffee shop name';
  //     ctx.instance.attendend = false;
  //   }
  //   next();
  // });
};
